# Week 1
## Hoare triples 和 Weakest precondition

为了验证代码的正确性，需要一个方法来验证
Hoare 引入了 Hoare triple model：
{P}  Precondition
S
{Q} Postcondition
如果系统P经过S之后系统到达了postcondition Q，那么就说这个系统是正确的
### Dijkstra improvement
Dijkstra 引入了Worst precondition这个概念，也就是在经过S能够到达Q的最弱的前提。
记作：WP.(x := x + 1).(5 ≤ x)  在这里是 {4 ≤ x}
为了记述这个记号，Dijkstra 引入了Guarded command language用来描述算法（相对严格的伪代码）

## Guarded Command Language
这是一个最小化的，过程性的语言。
首先先定义这个语言中的基本操作
0. Skip
   WP.skip.Q ≡ Q
   > 这里的意思是skip的定义就是由WP.skip.Q ≡ Q这个现象定义出的，读法应该是Weakest precondition of Skip to Q is Q,也就是通过weakest precondition这个东西来定义全部的操作，这里的WP其实都是倒推出来的
1. Concatenation
   WP.(R;S).Q ≡ WP.R.(WP.S.Q)
   这式子表示：要让 **R;S** 执行后满足 Q 等价于先算执行 **S** 之前必须满足什么，再算执行 **R** 之前必须满足什么
   
2. Assignment
   WP.(x := E).Q ≡ Q(all x replaced by E)
   执行将x赋值成E之后符合Q，那么执行前WP需要符合用 E 代替 x 后的 Q 成立
   这个定义有点别扭（因为实际上是根据effect反向定义出来的），但是这个看一个例子可以好懂一点
   ```
   S : x = x + 1
   Q : x > 5
   那么WP(x = x + 1, x > 5) =
   -> x + 1 > 5
   -> x > 4   
   通过反向代入可以算出WP
   ```
3. Selection(if..fi)
```
{P}
if B0 -> S0
[] B1 -> S1
fi
{Q}
```
   表示
```
P => B0 v B1
^
{P ^ B0} S0 {Q} ^ {P ^ B1} S1 {Q}
```
  
4. Repetition
```
{R}
 do B -> S od
{Q}
```
 表示
``` 
R ⇒ P
∧
{P ∧ B ∧ vf = VF} S {P ∧ vf < VF}
∧
P ∧ B ⇒ 0 < vf
∧
P ∧ ¬B ⇒ Q
```
# Week 2
## 符号
 j.0 x j.1 x ... j.N-1 = <1.x j : 2.0<= j < 3.N : f.j >
 其中后面的形式叫做quantified form
 
1代表连接各个元素的符号
2代表筛选出的元素集合
3代表元素的形式

## 对于quantified form可用的laws：
1. empty range law:  存在identity element.
〈 ⊕ j : 0 ≤ j < 0 : f.j 〉
= {empty range} ID⊕
2. one-point: 
〈 ⊕ j : j = i : f.j 〉
= {one-point} f.i
3. The split off a term law (展开)
〈 ⊕ j : 0 ≤ j < i+1 : f.j 〉 where 0≤ i < N
= {split off j = i term}
〈 ⊕ j : 0 ≤ j < i : f.j 〉 ⊕ f.i
4. Strengthening (用来转化出∧)
〈 ⊕ j : 0 ≤ j < i : f.j 〉 ∧ i = N
⇒ {Leibniz }2
〈 ⊕ j : 0 ≤ j < N : f.j 〉

Week3:
对于所有形为R = <+.j : a<j\<b : f.j>
可以通过
```
n,R := a, ID
  do n != b ->
     n,R := n+1, R + f.n
  od
```
转化出

条件式：
对于需要筛选的式子，比如所有even number
我们通过新加一个函数的方式实现：
R = <f.j : 0<= j < n: g.(f.j)>
g.x = 1 <= even.x
g.x = 0 <= odd.x

=> imply
<= if

推导一个program：
先写出postcondition
然后将post 转化为一个n = N形式的强化post(如果已经有$\land$就不需要)

(0) C.i = <转写postcondition>
(1) C.0
(2) C.n+1
Rewrite postcondition
Invariants
p.0: r = C.n
Guards
Establish invariants
将所有变量变成id
v-f
Loop body
program

构建loop body：
(n,R := n+1,E) E 是未知数
E = C.(n+1)
{case even.(f.n)}
E = C.n + 1 = R + 1

n,R := 0,0
; do n != N -> 
	if even.(f.n) -> n,R := n+1,R+1
	if odd.(f.n) -> n,R := n+1,R
	fi
od

# Week 4
Linear search:

# Week5 
这节课以一个
$$
Pre: f.0 < x \land x < f.N
$$$$
Post: f.i<=x \land x < f(i+1)
$$
讲述了一个post condition没法简单的用一个式子列出来的情况
也就是找一个i位置小于X下一位大于X的式子

Binary chop 是Binary search 的超集。如果Binary chop作用在一个有序list上，那就是binary search

它的基本思想是这样的：首先这个关系必须是一个二元的关系，也就是 x Z h $\land$ h $\neg Z$ 这样的关系，比如x = 0 和 y  != 0,然后在集合上开始进行二分查找：h为x+y/2，如果h符合x的关系那就x := h, y符合就 y:=h直到找到关系的转折点也就是y = x+1。

$$
f(S)=\sum_{i=1}^{n}\max_{x\in S} x_i,
\qquad S\subseteq V\subseteq \mathbb{R}_+^n.$$ 